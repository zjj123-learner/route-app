# 行程规划算法详解

> 配套代码：`optimizer.py`（暴力枚举 / 最近邻+2-opt）、`simanneal.py`（模拟退火）、`genetic.py`（遗传算法）
> 评测脚本：`benchmark.py`（三算法 + 暴力最优对比）
> 本文从"问题是什么"讲起，逐步到每一段代码，读完你能把三个算法都讲明白。

---

## 0. 我们在解决什么问题

用户输入一堆任务，例如：

```
明天上午9点去银行办卡
下午3点去学校接孩子放学（重要）
顺便去超市买菜
晚上7点前从驿站取快递回家
```

每个任务被解析成这样的结构（`parser.py` 产出）：

```python
{
    "name": "下午3点去学校接孩子放学（重要）",
    "place": "学校",
    "lat": 31.25, "lng": 121.49,      # 坐标(由高德补全)
    "priority": 3,                     # 1低 2中 3高
    "duration": 30,                    # 停留分钟数
    "earliest": 900,                   # 最早开始(当天第几分钟)
    "latest": 1080,                    # 最晚开始
    "fixed": 900,                      # 固定时间(接孩子不能迟到)
    "deadline": None,                  # 截止时间(取快递19点前)
    "day": 0,                          # 第几天(0=今天, 1=明天...)
}
```

我们的目标：**决定访问这些地点的顺序**，让"总代价"最小。
这不是普通旅行商问题（TSP），因为多了四个现实约束：

1. **时间窗**：任务有 `earliest`（最早）和 `latest`（最晚），早到要等，晚到要罚；
2. **固定预约**：`fixed` 的任务（接人/开会/考试）必须准时，不能迟到；
3. **截止时间**：`deadline` 的任务（取快递/寄件）必须在截止前完成；
4. **优先级**：重要的事（priority=3）应该尽量排前面。

所以这是一个 **带时间窗、优先级和惩罚的 TSP 变体**。
任务数少时（≤8）可以暴力找最优；任务多时（9~30）要靠启发式/元启发式算法。

---

## 1. 评价函数：所有算法的同一把"尺子"

不管用哪个算法，候选顺序好不好，都必须用同一个标准打分。
这把"尺子"就是 `optimizer.py` 里的 `evaluate_order`：

```python
def evaluate_order(order, start, opts):
    t = opts["start_min"]          # 当前时间, 从出发时间开始(如 08:00=480)
    travel = wait = penalty = 0
    arrivals = []
    prev = start                   # 上一个地点, 起点是"家"

    for i, task in enumerate(order):
        tt = travel_minutes(prev, task, opts)   # ① 路程时间
        travel += tt
        t += tt

        # ② 等待: 早到了就等到最早可开始时间
        wait_target = None
        if task["fixed"] is not None:
            wait_target = task["fixed"] - opts["fixed_early_buffer"]
        elif task["earliest"] is not None:
            wait_target = task["earliest"]
        if wait_target is not None and t < wait_target:
            wait += wait_target - t
            t = wait_target

        # ③ 迟到惩罚
        if task["fixed"] is not None and t > task["fixed"]:
            penalty += (t - task["fixed"]) * opts["fixed_late_penalty"]
        if task["latest"] is not None and t > task["latest"]:
            penalty += (t - task["latest"]) * opts["window_late_penalty"]
        done = t + task["duration"]
        if task["deadline"] is not None and done > task["deadline"]:
            penalty += (done - task["deadline"]) * opts["deadline_penalty"]

        arrivals.append({"task": task, "arrival": t, "depart": done, "travel": tt})
        # ④ 优先级: 越靠后的高优先级任务, 扣分越多
        penalty += (task["priority"] - 1) * opts["priority_weight"] * i
        t = done
        prev = task

    total = travel + wait * opts["wait_factor"] + penalty
    return {"total": total, "travel": travel, "wait": wait, "penalty": penalty, "arrivals": arrivals}
```

四个成本来源：

| 成本 | 含义 | 权重（`DEFAULTS`） |
|---|---|---|
| `travel` | 路程时间（真实路网或直线估算） | 1 分钟 = 1 分 |
| `wait` | 提前到达的等待时间 | ×0.5 |
| `penalty` | 迟到/超时/优先级扣分 | 每档 30~100 分/分钟 |
| `priority` | 高优先级任务被排得越靠后扣越多 | 每位置 ×3 分 |

关键参数（`optimizer.py` 顶部的 `DEFAULTS`）：

```python
DEFAULTS = {
    "mode": "walk",            # walk/drive
    "walk_speed": 4.5,         # 步行 km/h
    "drive_speed": 28,         # 驾车 km/h
    "start_min": 480,          # 默认 08:00 出发
    "wait_factor": 0.5,        # 等待时间折算
    "fixed_late_penalty": 100, # 固定预约迟到: 每分钟扣100
    "window_late_penalty": 30, # 时间窗迟到: 每分钟扣30
    "deadline_penalty": 50,    # 截止超时: 每分钟扣50
    "priority_weight": 3,      # 优先级每靠后一个位置扣3
    "fixed_early_buffer": 30,  # 固定任务提前30分钟到算正常
    "max_brute_force": 8,      # 多少任务以内用暴力枚举
    "buffer": 0,               # 每段路程额外预留分钟(防踩点)
}
```

**为什么必须共用同一个评价函数？**
因为三个算法的差别只在"怎么搜"，不在"怎么评"。
共用一把尺子，`benchmark.py` 才能公平地比出谁找的解更好。

`travel_minutes` 还做了一个重要的事：**有真实路网矩阵就用真实时间，没有就回退直线估算**：

```python
def travel_minutes(a, b, opts):
    tm = opts.get("time_matrix")      # 高德算好的两两距离表
    if tm is not None:
        key = (round(a["lat"],5), round(a["lng"],5), round(b["lat"],5), round(b["lng"],5))
        got = tm.get(key)
        if got is not None:
            return got[1] + opts.get("buffer", 0)   # 真实分钟 + 余量
    km = haversine_km(a, b)           # 回退: 球面直线距离
    speed = opts["drive_speed"] if opts["mode"] == "drive" else opts["walk_speed"]
    extra = 2 if opts["mode"] == "drive" else 3
    return round(km / speed * 60 + extra) + opts.get("buffer", 0)
```

---

## 2. 暴力枚举（baseline，n ≤ 8）

最朴素的想法：把 n 个任务的所有排列（n! 种）都算一遍评分，取最好的。

```python
elif n <= opts["max_brute_force"]:
    best = None
    for perm in permutations(range(n)):        # 生成 n! 种排列
        order = [tasks[i] for i in perm]
        ev = evaluate_order(order, start, opts)
        if best is None or ev["total"] < best[1]["total"]:
            best = (order, ev)
    order, ev = best
    method = "brute-force"
```

- 8 个任务：8! = 40320 种排列，电脑瞬间算完；
- 10 个任务：10! = 3628800，开始变慢；
- 12 个任务：12! ≈ 4.8 亿，彻底不可行。

所以 `max_brute_force = 8`：**8 个以内直接给最优解，超过 8 个才上启发式**。
暴力解是另外两个算法的"照妖镜"——`benchmark.py` 里就用它来量化别的算法离最优差多少。

---

## 3. 启发式：最近邻 + 2-opt

### 3.1 最近邻（Nearest Neighbor）：贪心构造初始解

思路：从起点出发，每一步都挑"当前综合代价最小"的下一个任务，
直到走完所有任务。这是贪心——**每一步都选眼前最好的，不管全局**。

```python
def nearest_neighbor(tasks, start, opts):
    remaining = list(tasks)
    order = []
    prev = start
    clock = opts["start_min"]
    while remaining:
        best_i, best_cost = 0, None
        for i, t in enumerate(remaining):
            tt = travel_minutes(prev, t, opts)          # 路程
            wait = 0
            target = None
            if t["fixed"] is not None:
                target = t["fixed"] - opts["fixed_early_buffer"]
            elif t["earliest"] is not None:
                target = t["earliest"]
            if target is not None and clock + tt < target:
                wait = target - (clock + tt)            # 提前到要等多久
            cost = tt + wait + (t["priority"] - 1) * opts["priority_weight"]
            if best_cost is None or cost < best_cost:
                best_cost, best_i = cost, i
        t = remaining.pop(best_i)
        order.append(t)
        prev = t
        clock += best_cost + t["duration"]              # 时间推进
    return order
```

贪心的毛病：**先做的选择会锁死后面的路**。
比如先去了城南的银行，结果发现最要紧的学校在城北，来回绕远。

### 3.2 2-opt + 交换：局部搜索精修

有了一个初始解之后，用"局部搜索"不断改进：
每次尝试两种小改动（邻域），评分变好就接受，反复直到没有改进。

```python
def two_opt(order, start, opts, max_passes=20, fixed_slots=()):
    fixed_slots = set(fixed_slots)
    best_order = list(order)
    best_total = evaluate_order(best_order, start, opts)["total"]
    for _ in range(max_passes):
        improved = False
        # 邻域1: 反转一段 order[i..j] (经典 2-opt)
        for i in range(n):
            for j in range(i + 1, n):
                if any(s in fixed_slots for s in range(i, j + 1)):
                    continue
                candidate = best_order[:i] + best_order[i:j+1][::-1] + best_order[j+1:]
                total = evaluate_order(candidate, start, opts)["total"]
                if total < best_total:
                    best_order, best_total = candidate, total
                    improved = True
                    break
            if improved: break
        if improved: continue
        # 邻域2: 交换两个位置
        for i in range(n):
            for j in range(i + 1, n):
                if i in fixed_slots or j in fixed_slots: continue
                candidate = list(best_order)
                candidate[i], candidate[j] = candidate[j], candidate[i]
                total = evaluate_order(candidate, start, opts)["total"]
                if total < best_total:
                    best_order, best_total = candidate, total
                    improved = True
                    break
            if improved: break
        if not improved: break     # 一轮没有改进就停
    return best_order
```

"2-opt" 名字来自：**去掉两条边，用交叉的方式重新连上两条边**，
对一条路线等价于"反转其中一段"。它只接受变好的解，所以是**确定性**的。

### 3.3 启发式的局限

`heuristic_route` = 最近邻构造 + 2-opt 精修：

```python
def heuristic_route(tasks, start, opts):
    order = nearest_neighbor(tasks, start, opts)
    order = two_opt(order, start, opts)
    ev = evaluate_order(order, start, opts)
    return order, ev
```

它的致命伤：**只会往好的方向走，一旦卡进"局部最优"（一个周围全是更差解的坑）就出不来**。
`benchmark.py` 里 7 个任务的实例出现过 12%~17% 的差距，就是卡坑了。

---
## 4. 模拟退火（Simulated Annealing）

### 4.1 灵感：金属退火

冶金里，把金属烧热再慢慢冷却，原子会逐渐落到能量最低的稳定结构。
模拟退火把这个过程搬进算法：

- **温度高**时，允许接受"更差的解"（原子乱动，探索全局）；
- **温度低**时，几乎只接受"更好的解"（原子稳定，收敛细节）；
- 温度从高到低，这就是"退火"。

对应到我们的问题：
- "解" = 一个任务顺序；
- "邻居" = 随机交换两个位置 / 反转一段；
- "能量" = `evaluate_order` 的 `total`；
- "温度" = 一个逐渐变小的数。

### 4.2 核心：Metropolis 准则

每次生成一个邻居解，用这个公式决定要不要接受：

```
Δ = 新解总分 - 当前总分
接受概率 = 1          (Δ < 0, 变好了, 直接接受)
接受概率 = e^(-Δ/T)   (Δ > 0, 变差了, 以这个概率"赌一把")
```

温度 T 越大，接受差解的概率越大。代码里就是这样：

```python
delta = total - cur_total
if delta < 0 or rng.random() < math.exp(-delta / T):
    cur_free, cur_total = cand, total      # 接受(不管好坏)
    if total < best_total:                 # 但历史最优单独记
        best_free, best_total = list(cand), total
```

注意：**历史最优单独记录**。所以即使中途"走歪"了，最后返回的也是见过的最好解。

### 4.3 温度和迭代：自适应

温度不能拍脑袋定。我们第一版写死 `t0 = 50`，结果在惩罚值很大的实例上
**退化成爬山**（差解的接受概率趋近 0，跳不出坑），8 任务比最优差了 25%。

修法：初始温度取"初始解总代价的 10%"——代价越大，初始越"热"，自动适配：

```python
# 初始解: 用最近邻构造(保证起点不差), 再交给退火去精修/跳坑
cur_free = nearest_neighbor(free_tasks, start, opts)
best_free = list(cur_free)
best_total = evaluate_order(_full_order(base, free_slots, best_free), start, opts)["total"]
cur_total = best_total

if iters is None:
    iters = max(4000, n * 1200)        # 迭代次数随任务数增长
if t0 is None:
    t0 = max(10.0, best_total * 0.1)   # 初始温度 = 初始总代价的10%
if alpha is None:
    alpha = (0.01 / t0) ** (1.0 / iters)   # 保证最后温度≈0.01

rng = random.Random(seed)              # 固定种子, 结果可复现
for k in range(iters):
    T = t0 * (alpha ** k)              # 几何降温: T = t0 * alpha^k
    cand = _neighbor(cur_free, rng)
    total = evaluate_order(_full_order(base, free_slots, cand), start, opts)["total"]
    ...
```

`_neighbor` 生成邻居：一半概率交换两个位置，一半概率反转一段（借了 2-opt 的思路）：

```python
def _neighbor(free, rng):
    n = len(free)
    if n < 2: return list(free)
    i, j = rng.randrange(n), rng.randrange(n)
    if i == j: j = (j + 1) % n
    if i > j: i, j = j, i
    if rng.random() < 0.5:
        cand = list(free); cand[i], cand[j] = cand[j], cand[i]   # 交换
    else:
        cand = free[:i] + free[i:j+1][::-1] + free[j+1:]         # 反转
    return cand
```

### 4.4 和 2-opt 的本质区别

| | 2-opt | 模拟退火 |
|---|---|---|
| 走法 | 只接受变好 | 变好全收，变差按概率收 |
| 风险 | 卡局部最优 | 前期能"先差后好"跳出坑 |
| 确定性 | 是 | 否（固定 seed 后可复现） |

---

## 5. 遗传算法（Genetic Algorithm）

### 5.1 灵感：进化论

把候选路线当成"生物个体"，让它们"繁殖进化"：
适应度高的个体更容易留下后代，后代继承父母的基因并发生变异，
一代代迭代，整个种群慢慢逼近最优。

### 5.2 五个要素对应到代码

| 进化论概念 | 本问题的实现 |
|---|---|
| 染色体 | 一个任务顺序（`[0,2,1,3]` 表示先任务0再任务2…） |
| 种群 | 60 条不同的候选路线 |
| 适应度 | `evaluate_order` 的 total（越小越好） |
| 选择 | 锦标赛：随机抽 3 个，留下最好的当父母 |
| 交叉 | 顺序交叉 OX：爸爸的一段 + 妈妈补全其余 |
| 变异 | 小概率交换/反转一段 |
| 精英保留 | 每代最好的 2 个直接进下一代，防止退化 |

### 5.3 主循环

```python
pop = []
for i in range(pop_size):
    if i == 0:
        pop.append(list(nn_idx))                 # 第1个个体=最近邻解, 保证起点质量
    else:
        chromo = list(range(m)); rng.shuffle(chromo)   # 其余随机
        pop.append(chromo)

best_total, best_chromo = None, None
for _ in range(generations):
    scored = sorted((fitness(c), c) for c in pop)      # 按适应度排序
    if best_total is None or scored[0][0] < best_total:
        best_total = scored[0][0]; best_chromo = list(scored[0][1])
    next_pop = [list(c) for _, c in scored[:elite]]    # 精英保留
    while len(next_pop) < pop_size:
        p1 = _tournament(scored, tournament, rng)      # 选父母
        p2 = _tournament(scored, tournament, rng)
        child = _crossover(p1, p2, rng)                # 生小孩
        if rng.random() < mutation_rate:
            child = _mutate(child, rng)                # 变异
        next_pop.append(child)
    pop = next_pop                                     # 进入下一代
```

`fitness` 和评价函数的关系：**就是 `evaluate_order` 的 total，一分不差**。

```python
def fitness(chromo):
    order = _full_order(base, free_slots, [free_tasks[i] for i in chromo])
    return evaluate_order(order, start, opts)["total"]
```

### 5.4 三个算子

**锦标赛选择**：随机抽 k 个比一比，最好的当父母（每次抽的样本不同，所以"差"个体也有小概率留下，保持多样性）：

```python
def _tournament(scored, k, rng):
    picks = rng.sample(scored, min(k, len(scored)))
    return min(picks, key=lambda x: x[0])[1]
```

**顺序交叉 OX**：从爸爸 a 里取一段"原样复制"给小孩，其余位置按妈妈 b 的顺序填，
保证小孩仍然是"一个完整的排列"（每个任务出现且只出现一次）：

```python
def _crossover(a, b, rng):
    m = len(a)
    if m < 2: return list(a)
    i = rng.randrange(m - 1); j = rng.randrange(i + 1, m)
    child = [None] * m
    child[i:j+1] = a[i:j+1]                  # 爸爸的一段
    pos = (j + 1) % m
    for gene in b:                           # 妈妈按顺序补全
        if gene not in child:
            child[pos] = gene; pos = (pos + 1) % m
    return child
```

**变异**：交换或反转，防止种群过早"同质化"：

```python
def _mutate(chromo, rng):
    m = len(chromo)
    if m < 2: return chromo
    i, j = rng.randrange(m), rng.randrange(m)
    if i == j: return chromo
    if i > j: i, j = j, i
    if rng.random() < 0.5:
        chromo[i], chromo[j] = chromo[j], chromo[i]
    else:
        chromo[i:j+1] = reversed(chromo[i:j+1])
    return chromo
```

---

## 6. 三个算法怎么共用一套代码框架

`simanneal.py` 和 `genetic.py` 都实现了和 `optimize_route` 一样的返回结构：

```python
{
    "order": [...],          # 最优顺序(任务对象列表)
    "arrivals": [...],       # 每个任务的到达/离开时间
    "stats": {"total":..., "travel":..., "wait":..., "penalty":...},
    "method": "simanneal",   # 或 "genetic" / "brute-force" / "heuristic"
}
```

所以 `app.py` 里一次请求把三个算法都跑一遍，只是换函数名：

```python
route_results = {
    "heuristic": optimize_route(tasks, start, options, fixed_positions=fixed_positions or None),
    "simanneal": sa_route(tasks, start, options, fixed_positions=fixed_positions or None, seed=20260826),
    "genetic":   ga_route(tasks, start, options, fixed_positions=fixed_positions or None, seed=20260826),
}
```

**路网矩阵只算一次**（`route.build_matrix`），三个算法都查同一张表，高德请求不翻倍。

**锁定任务**：三个算法都通过 `_build_base` 把锁定任务钉在固定槽位，
只对自由任务做排列——所以"锁定"功能在三个算法下行为一致。

---

## 7. 实测对比（benchmark.py）

随机生成 40 个任务集（5~12 个任务，每规模 5 个实例，固定种子 20260825，直线估算）：

```
小规模(5~8任务) 离暴力最优的平均差距:
    启发式  2.24%   ← 会卡局部最优
    模拟退火 0.00%   ← 20个实例全部找到最优
    遗传    0.00%   ← 同样全部找到最优

大规模(9~12任务, 暴力不可行) 谁找到的总分最低:
    遗传 4 次 / 模拟退火 1 次 / 启发式 0 次 / 打平 15 次
```

结论要点（写报告可以直接用）：
1. **启发式快但会卡坑**：小规模就平均差 2.24%，个别实例差 12%~17%；
2. **模拟退火和遗传都明显优于纯启发式**，小规模都能逼近最优；
3. **两者互有胜负**（取决于实例和随机种子），没有谁全面碾压——这也是报告里很诚实的结论；
4. 任务 ≤8 时前端会提示"三个算法结果一致，已是最优"，因为暴力枚举直接给出最优。

---

## 8. 怎么选 / 还能怎么改进

| 场景 | 用谁 |
|---|---|
| 任务 ≤ 8 | 暴力枚举（就是最优） |
| 任务 9~30，要稳定 | 模拟退火（参数少、行为好解释） |
| 任务 9~30，想演示"进化" | 遗传算法（概念酷、可并行） |
| 只要最快 | 启发式（毫秒级，但可能差一点） |

可改进方向（进阶话题）：
- **混合算法（Memetic）**：遗传每代结束对最优个体再跑一遍 2-opt，收敛更快；
- **多起点**：模拟退火/遗传从多个不同初始解开始，取最好；
- **OR-Tools CP-SAT**：Google 工业级求解器，任务多/约束复杂时更强，但它是黑盒，讲不清内部原理；
- **实时调整**：前端"完成/跳过"后只对剩余任务重新规划，复用已有解当初始解。

---

## 9. 常见问题

**Q1：为什么三个按钮的结果经常一模一样？**
因为 ≤8 个任务走的是暴力枚举（最优解），退火/遗传也会收敛到最优，三者自然一致。

**Q2：为什么退火/遗传的结果每次可能不同？**
它们是随机算法。代码里固定了 `seed`（`sa_route(..., seed=20260826)`），
所以同一批输入结果可复现；换 seed 结果会变。报告里建议说明"固定随机种子保证实验可复现"。

**Q3：评价函数的数值是"分钟"吗？**
不全是：路程/等待是分钟，但迟到惩罚是"分钟×权重"（100/30/50），
优先级是"位置×3"。所以 `total` 是"加权代价"，不是真实分钟数——
它只用来**比较**顺序的好坏，前端显示的真实总耗时另算（`summary.total_minutes`）。

**Q4：为什么 2-opt 明明会反转一段，名字却叫"2-opt"？**
TSP 里"2-opt"指去掉两条边、交叉重连；对一条闭合/开环路线等价于反转中间一段。
我们借用了这个经典操作，所以沿用名字。

**Q5：锁定任务怎么和这些算法兼容的？**
三个算法都先把锁定任务放进固定槽位（`_build_base`），
只对剩余"自由任务"做排列/交叉/变异，锁定永不移动。

---

*生成于 route-app 项目开发过程，配套代码版本见 git log。*
